# HydroDocAI environment template. Copy to .env and fill in values.
# Never commit .env or bake it into Docker images (see docs/docker-env-config.md).

# --- Backend (runtime) ---
# LLM API key (required for generation). e.g. SiliconFlow / Gemini.
API_KEY=

# Optional: LLM base URL (default: https://api.siliconflow.cn/v1)
# SILICONFLOW_BASE_URL=

# Optional: LLM model (default: Qwen/Qwen3-8B)
# LLM_MODEL=

# Optional: Backend port (default: 4001)
# PORT=4001

# Optional: CORS allowed origin. Set to frontend URL in production (e.g. https://your-frontend.domain).
# CORS_ORIGIN=

# --- Frontend (build-time only for Next.js) ---
# Backend API base URL. Set when building frontend for production (e.g. https://api.yourdomain.com).
# NEXT_PUBLIC_API_URL=
